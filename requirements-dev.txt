# Dev / CI dependencies — install on top of requirements.txt
-r requirements.txt

# Testing
pytest>=7.4.0,<9.0
pytest-asyncio>=0.23.0,<1.0
httpx>=0.25.0,<1.0

# Linting
ruff>=0.1.0,<1.0

# Type checking
mypy>=1.7.0,<2.0
